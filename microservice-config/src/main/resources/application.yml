server:
    port: 8080
spring:
  profiles:
    activate: native
  aplication:
    name: config-server
  cloud:
    config:
      server:
        native:
          search-locations: file:///Users/username/config-repo
